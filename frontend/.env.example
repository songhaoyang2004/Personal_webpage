# 生产环境在 Vercel 等构建环境里填：你的简历 API 公网地址（无末尾斜杠）
# 本地 npm run dev / npm run preview：可留空，Vite 会把 /api 代理到 http://127.0.0.1:18080
# 若用 python -m http.server 等直接打开 dist、且未走 Vite preview，请填：
# VITE_API_URL=http://127.0.0.1:18080
VITE_API_URL=
